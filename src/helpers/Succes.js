const Succes = (msg) => {
    return (
            <div>
                <div className="alert alert-success" role="alert">
                {msg}
                </div>
            </div> 
        )
}

export default Succes;
